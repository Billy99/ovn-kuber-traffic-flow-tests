---
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: web-client-pod
spec:
  selector:
    matchLabels:
      name: web-client-pod
  template:
    metadata:
      labels:
        name: web-client-pod
    spec:
      containers:
      - name: web-client-pod
        image: registry.access.redhat.com/ubi8/ubi
        command:
          - /sbin/init
        imagePullPolicy: IfNotPresent
      nodeSelector:
        ft.ClientPod: client
